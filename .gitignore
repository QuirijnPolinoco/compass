# Rust build output
/target
**/target/
**/*.rs.bk
*.pdb

# Compass's per-repo cache/state (written into mapped repos)
.compass/

# Per-repo MCP config + local agent settings — machine-specific (absolute paths), never publish
.mcp.json
.claude/settings.local.json

# Local planning-board state from an external tool — not part of Compass
.waypoint/

# Editor / OS noise
.DS_Store
.idea/
.vscode/
*.swp

# Note: Cargo.lock IS committed — Compass ships as a binary, so we pin exact deps.
