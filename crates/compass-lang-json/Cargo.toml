[package]
name = "compass-lang-json"
description = "Compass — JSON catalog extractor (record keys, and OpenAPI / JSON Schema / Postman names as symbols; values are never indexed)."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
compass-core.workspace = true
compass-extract.workspace = true
tree-sitter.workspace = true
tree-sitter-json.workspace = true
